# 프로젝트 구조 및 역할별 정리

## 📁 프로젝트 구조

```
shopping-mall-demo/
├── client/                    # React 프론트엔드
│   ├── public/               # 정적 파일
│   │   └── camera.jpg        # 배너 이미지
│   ├── src/
│   │   ├── config/           # 설정 파일
│   │   │   └── api.js        # API 엔드포인트 설정
│   │   ├── contexts/         # React Context
│   │   │   └── UserContext.jsx  # 사용자 인증 상태 관리
│   │   ├── components/       # 공통 컴포넌트
│   │   │   ├── Navbar.jsx    # 네비게이션 바
│   │   │   └── Navbar.css
│   │   ├── pages/            # 페이지 컴포넌트
│   │   │   ├── admin/        # 관리자 전용 페이지
│   │   │   │   ├── Admin.jsx              # 상품 관리 메인
│   │   │   │   ├── AdminProductNew.jsx    # 상품 등록/수정
│   │   │   │   ├── AdminOrder.jsx         # 주문 관리
│   │   │   │   └── AdminUser.jsx          # 회원 관리
│   │   │   ├── Home.jsx                   # 메인 페이지
│   │   │   ├── Login.jsx                  # 로그인
│   │   │   ├── Register.jsx               # 회원가입
│   │   │   ├── ProductDetail.jsx          # 상품 상세
│   │   │   ├── CategoryPage.jsx           # 카테고리별 상품 목록
│   │   │   ├── Cart.jsx                   # 장바구니
│   │   │   ├── Checkout.jsx               # 결제 페이지
│   │   │   ├── OrderComplete.jsx           # 주문 완료
│   │   │   ├── OrderList.jsx               # 주문 목록
│   │   │   ├── OrderDetail.jsx             # 주문 상세
│   │   │   └── MyPage.jsx                 # 마이페이지
│   │   ├── utils/            # 유틸리티 함수
│   │   │   ├── api.js        # API 클라이언트
│   │   │   ├── errorHandler.js  # 에러 처리
│   │   │   └── constants.js      # 상수 정의
│   │   ├── App.jsx           # 라우터 설정
│   │   └── main.jsx          # 진입점
│   └── package.json
│
└── server/                    # Express 백엔드
    ├── config/                # 설정 파일
    │   └── database.js        # MongoDB 연결
    ├── models/                # Mongoose 스키마
    │   ├── User.js            # 사용자 모델
    │   ├── Product.js         # 상품 모델
    │   ├── Cart.js            # 장바구니 모델
    │   └── Order.js           # 주문 모델
    ├── controllers/           # 비즈니스 로직
    │   ├── authController.js   # 인증 (로그인)
    │   ├── userController.js   # 사용자 관리
    │   ├── productController.js # 상품 관리
    │   ├── cartController.js   # 장바구니 관리
    │   └── orderController.js  # 주문 관리
    ├── routes/                # API 라우트
    │   ├── index.js           # 라우트 통합
    │   ├── auth.js            # 인증 라우트
    │   ├── users.js           # 사용자 라우트
    │   ├── products.js        # 상품 라우트
    │   ├── cart.js            # 장바구니 라우트
    │   └── orders.js          # 주문 라우트
    ├── middleware/            # 미들웨어
    │   └── auth.js            # JWT 인증 미들웨어
    ├── app.js                 # Express 앱 설정
    ├── index.js               # 서버 진입점
    └── package.json
```

## 🎭 역할별 기능 정리

### 1. 인증 (Authentication)

#### 백엔드
- **모델**: `server/models/User.js`
  - 이메일, 비밀번호, 사용자 타입 (CUSTOMER/ADMIN)
  - 비밀번호는 bcrypt로 해시화

- **컨트롤러**: `server/controllers/authController.js`
  - `login`: 로그인 처리, JWT 토큰 발급
  - `getMe`: 현재 로그인한 사용자 정보 조회

- **라우트**: `server/routes/auth.js`
  - `POST /api/auth/login`: 로그인
  - `GET /api/auth/me`: 현재 사용자 정보 (인증 필요)

- **미들웨어**: `server/middleware/auth.js`
  - JWT 토큰 검증
  - `req.user`에 사용자 정보 추가

#### 프론트엔드
- **Context**: `client/src/contexts/UserContext.jsx`
  - 전역 사용자 상태 관리
  - 로그인/로그아웃 함수 제공
  - 토큰 자동 관리

- **페이지**:
  - `Login.jsx`: 로그인 폼
  - `Register.jsx`: 회원가입 폼

---

### 2. 사용자 관리 (User Management)

#### 백엔드
- **컨트롤러**: `server/controllers/userController.js`
  - `getAllUsers`: 전체 사용자 조회 (페이징, 검색, 필터링)
  - `getUserById`: ID로 사용자 조회
  - `getUserByEmail`: 이메일로 사용자 조회
  - `createUser`: 새 사용자 생성
  - `updateUser`: 사용자 정보 수정
  - `deleteUser`: 사용자 삭제

- **라우트**: `server/routes/users.js`
  - `GET /api/users`: 전체 사용자 조회
  - `GET /api/users/:id`: 사용자 조회
  - `PUT /api/users/:id`: 사용자 수정
  - `DELETE /api/users/:id`: 사용자 삭제

#### 프론트엔드
- **일반 사용자**:
  - `MyPage.jsx`: 내 정보 수정, 주문 목록

- **관리자**:
  - `AdminUser.jsx`: 회원 목록, 권한 변경, 회원 삭제

---

### 3. 상품 관리 (Product Management)

#### 백엔드
- **모델**: `server/models/Product.js`
  - 상품번호, 이름, 이미지, 카테고리, 가격, 설명

- **컨트롤러**: `server/controllers/productController.js`
  - `getAllProducts`: 전체 상품 조회 (페이징, 검색, 카테고리 필터)
  - `getProductById`: ID로 상품 조회
  - `getProductByNumber`: 상품번호로 조회
  - `createProduct`: 상품 등록
  - `updateProduct`: 상품 수정
  - `deleteProduct`: 상품 삭제

- **라우트**: `server/routes/products.js`
  - `GET /api/products`: 전체 상품 조회
  - `GET /api/products/:id`: 상품 조회
  - `POST /api/products`: 상품 등록 (인증 필요)
  - `PUT /api/products/:id`: 상품 수정 (인증 필요)
  - `DELETE /api/products/:id`: 상품 삭제 (인증 필요)

#### 프론트엔드
- **일반 사용자**:
  - `Home.jsx`: 메인 페이지, 상품 목록
  - `ProductDetail.jsx`: 상품 상세 정보, 장바구니 추가, 바로구매
  - `CategoryPage.jsx`: 카테고리별 상품 목록

- **관리자**:
  - `Admin.jsx`: 상품 목록, 통계
  - `AdminProductNew.jsx`: 상품 등록/수정 (Cloudinary 이미지 업로드)

---

### 4. 장바구니 (Shopping Cart)

#### 백엔드
- **모델**: `server/models/Cart.js`
  - 사용자, 상품, 수량
  - 사용자별 동일 상품 중복 방지 (unique index)

- **컨트롤러**: `server/controllers/cartController.js`
  - `getCart`: 사용자 장바구니 조회
  - `addToCart`: 장바구니에 상품 추가
  - `updateCartItem`: 장바구니 항목 수량 수정
  - `removeFromCart`: 장바구니에서 상품 제거
  - `clearCart`: 장바구니 비우기

- **라우트**: `server/routes/cart.js`
  - `GET /api/cart`: 장바구니 조회 (인증 필요)
  - `POST /api/cart`: 상품 추가 (인증 필요)
  - `PUT /api/cart/:id`: 수량 수정 (인증 필요)
  - `DELETE /api/cart/:id`: 상품 제거 (인증 필요)
  - `DELETE /api/cart/clear`: 장바구니 비우기 (인증 필요)

#### 프론트엔드
- **페이지**:
  - `Cart.jsx`: 장바구니 목록, 수량 수정, 상품 삭제, 주문하기

- **컴포넌트**:
  - `Navbar.jsx`: 장바구니 아이콘, 실시간 수량 표시

---

### 5. 주문 관리 (Order Management)

#### 백엔드
- **모델**: `server/models/Order.js`
  - 주문번호, 사용자, 상품 목록, 배송 정보, 금액, 상태, 결제 정보

- **컨트롤러**: `server/controllers/orderController.js`
  - `getOrders`: 주문 목록 조회 (필터링, 페이징)
  - `getOrderById`: 주문 상세 조회
  - `createOrder`: 주문 생성 (결제 검증 포함)
  - `updateOrder`: 주문 수정 (상태 변경 등)
  - `deleteOrder`: 주문 삭제/취소
  - `getIamportToken`: 포트원 API 토큰 발급
  - `verifyPayment`: 포트원 결제 검증
  - `checkDuplicateOrder`: 중복 주문 체크

- **라우트**: `server/routes/orders.js`
  - `GET /api/orders`: 주문 목록 조회 (인증 필요)
  - `GET /api/orders/:id`: 주문 상세 조회 (인증 필요)
  - `POST /api/orders`: 주문 생성 (인증 필요)
  - `PUT /api/orders/:id`: 주문 수정 (인증 필요)
  - `DELETE /api/orders/:id`: 주문 취소 (인증 필요)

#### 프론트엔드
- **일반 사용자**:
  - `Checkout.jsx`: 결제 페이지 (포트원 결제 연동)
  - `OrderComplete.jsx`: 주문 완료 페이지
  - `OrderList.jsx`: 내 주문 목록 (상태별 필터링)
  - `OrderDetail.jsx`: 주문 상세 정보, 주문 취소

- **관리자**:
  - `AdminOrder.jsx`: 전체 주문 목록, 상태 변경, 통계

---

### 6. 관리자 기능 (Admin Features)

#### 권한 체크
- 모든 관리자 페이지에서 `user.userType === 'ADMIN'` 체크
- 권한 없으면 메인 페이지로 리다이렉트

#### 관리자 페이지
1. **상품 관리** (`Admin.jsx`)
   - 상품 목록, 통계, 삭제
   - 상품 등록/수정 페이지로 이동

2. **주문 관리** (`AdminOrder.jsx`)
   - 전체 주문 목록, 상태별 필터링
   - 주문 상태 변경
   - 주문 통계

3. **회원 관리** (`AdminUser.jsx`)
   - 회원 목록, 검색, 필터링
   - 회원 권한 변경
   - 회원 삭제

4. **상품 등록/수정** (`AdminProductNew.jsx`)
   - Cloudinary 이미지 업로드
   - 상품 정보 입력/수정

---

## 🔐 보안 고려사항

### 현재 구현된 보안 기능
1. ✅ 비밀번호 해시화 (bcrypt)
2. ✅ JWT 토큰 인증
3. ✅ 관리자 권한 체크
4. ✅ 결제 검증 (포트원 API)
5. ✅ 중복 주문 방지

### 개선 필요 사항
1. ⚠️ 환경 변수 관리 (`.env` 파일 생성 필요)
2. ⚠️ CORS 설정 검토
3. ⚠️ 입력 데이터 검증 강화
4. ⚠️ Rate Limiting 추가 고려

---

## 📝 API 엔드포인트 요약

### 인증
- `POST /api/auth/login` - 로그인
- `GET /api/auth/me` - 현재 사용자 정보

### 사용자
- `GET /api/users` - 사용자 목록
- `GET /api/users/:id` - 사용자 조회
- `PUT /api/users/:id` - 사용자 수정
- `DELETE /api/users/:id` - 사용자 삭제

### 상품
- `GET /api/products` - 상품 목록
- `GET /api/products/:id` - 상품 조회
- `POST /api/products` - 상품 등록
- `PUT /api/products/:id` - 상품 수정
- `DELETE /api/products/:id` - 상품 삭제

### 장바구니
- `GET /api/cart` - 장바구니 조회
- `POST /api/cart` - 상품 추가
- `PUT /api/cart/:id` - 수량 수정
- `DELETE /api/cart/:id` - 상품 제거
- `DELETE /api/cart/clear` - 장바구니 비우기

### 주문
- `GET /api/orders` - 주문 목록
- `GET /api/orders/:id` - 주문 조회
- `POST /api/orders` - 주문 생성
- `PUT /api/orders/:id` - 주문 수정
- `DELETE /api/orders/:id` - 주문 취소

---

## 🚀 실행 방법

### 개발 환경
```bash
# 전체 설치
npm run install:all

# 서버와 클라이언트 동시 실행
npm run dev

# 또는 각각 실행
npm run dev:server  # 서버만
npm run dev:client  # 클라이언트만
```

### 프로덕션 빌드
```bash
# 클라이언트 빌드
npm run build

# 서버 실행
npm start
```

---

## 📌 주요 개선 사항

1. ✅ API URL 환경 변수화
2. ✅ 공통 에러 핸들링 유틸리티 생성
3. ✅ 상수 정의 파일 생성
4. ✅ API 클라이언트 중앙화
5. ⏳ 디버깅 코드 정리 (진행 예정)
6. ⏳ 환경 변수 파일 생성 (진행 예정)
